// @flow
import React from 'react';
import ChopWrapper from '../components/ChopWrapper';

type propTypes = {}

const HomeContainer = (props: propTypes) => (
  <ChopWrapper>
    <h1>홈</h1>
  </ChopWrapper>
);

export default HomeContainer;
