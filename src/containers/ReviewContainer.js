// @flow
import React from 'react';
import ChopWrapper from '../components/ChopWrapper';

type propTypes = {}

const ReviewContainer = (props: propTypes) => (
  <ChopWrapper>
    리뷰
  </ChopWrapper>
);

export default ReviewContainer;
