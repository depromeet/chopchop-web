// @flow
import React from 'react';
import ChopWrapper from '../components/ChopWrapper';

type propTypes = {}

const RoomContainer = (props: propTypes) => (
  <ChopWrapper>
    <h1>룸</h1>
  </ChopWrapper>
);

export default RoomContainer;
