// @flow
import React from 'react';
import ChopWrapper from '../components/ChopWrapper';

type propTypes = {}

const MyPageContainer = (props: propTypes) => (
  <ChopWrapper>
    <h1>마이페이지</h1>
  </ChopWrapper>
);

export default MyPageContainer;
