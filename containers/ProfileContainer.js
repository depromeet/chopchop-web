// @flow
import React from 'react';
import ChopWrapper from '../components/ChopWrapper';

type propTypes = {}

const ProfileContainer = (props: propTypes) => (
  <ChopWrapper>
    <h1>프로필</h1>
  </ChopWrapper>
);

export default ProfileContainer;
